```{ojs}
//| output: false
db = {
  const StratumSQLite = await window._stratumSQLite;
  return StratumSQLite.open(window._dbRefined, {
    sqlJsPath: window._sqljsBase,
    cacheKey:  "multised-refined@v0.2.0",
  });
}
```
